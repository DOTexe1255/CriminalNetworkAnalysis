import "./InvestigationHeader.css";
type Props = { query: string; onQueryChange: (value: string) => void; onQuerySubmit: () => void; onSummary: () => void; caseId: string; onCaseChange: (value: string) => void; onBack?: () => void };

export default function InvestigationHeader({ query, onQueryChange, onQuerySubmit, onSummary, caseId, onCaseChange, onBack }: Props) {
	return <header className="trace-header"><button className="header-back" onClick={onBack} disabled={!onBack} aria-label="Back to case register">←</button><div className="trace-brand"><div className="trace-mark">T</div><div><strong>TRACE</strong><span>CRIMINAL NETWORK INTELLIGENCE</span></div></div><div className="case-selector"><span>CASE</span><button onClick={() => onCaseChange(caseId)}>{caseId} ⌄</button></div><label className="entity-search"><span>⌕</span><input value={query} onChange={e => onQueryChange(e.target.value)} onKeyDown={e => { if (e.key === "Enter") onQuerySubmit(); }} placeholder="Ask about this case..." /><kbd>↵ ASK</kbd></label><button className="header-action" onClick={onQuerySubmit}>ASK CASE</button><button className="header-action" onClick={onSummary}>SUMMARY</button><button className="header-icon">⋮</button></header>;
}
